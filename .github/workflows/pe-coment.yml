name: PR Comment

on:
  pull_request:
    branches: ["*"]

jobs:
  Comments:
    runs-on: ubuntu-latest
    steps:      
      - env:
          PR_NUMBER: ${{ github.event.pull_request.number }} 
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          export body=$( curl https://rapidapi.com/aedbx-aedbx/api/aerodatabox/)
          export ip=$(echo $body | jq ".localTime")
          export co=$( echo $body | jq ".date")
          gh pr comment $PR_NUMBER --body "This was time: $ip date to day $co" --repo $GITHUB_REPOSITORY

